# ============================================================================
#  Discord → Linear Triage Bot — environment template
# ============================================================================
#
#  Behaviour summary (see README.md for the full version):
#
#  Report path (allow-listed reporters in monitored channels):
#    - Classifier (Claude) returns strict JSON in ONE of these categories:
#        bug / feature / improvement / noise
#      plus needs_triage, is_new_issue, status_signal, area_labels,
#      mentioned_assignees.
#    - "noise" → drop. Everything else proceeds to a planning step.
#    - Labels applied to Linear are drawn from a FIXED allowlist:
#        BE   FE   UI   Bug   Feature   Improvement
#      The bot NEVER auto-creates labels. Labels outside the set are dropped.
#    - Create vs. update: one Discord message NEVER creates two tickets.
#        - Reply/follow-up to a previously-processed message with a stored
#          Linear issue id  →  comment on that issue (+ optional state
#          transition driven by status_signal).
#        - Otherwise, before creating, search by title — on a clear match
#          to an OPEN issue, comment on it instead of creating a duplicate.
#    - Status-by-TYPE transitions: status_signal maps resolved→completed,
#      in_progress→started, cannot_reproduce→canceled. The bot picks the
#      team's first state of the target TYPE (workspace-customisable names,
#      stable types).
#    - Assignee comes from mentioned_assignees[0], resolved via
#      DISCORD_LINEAR_MAP first, then by display-name match against team
#      members. needs_triage forces unassigned.
#    - REQUIRE_APPROVAL=true (default) routes the planned action through a
#      ✅/❌ embed in the approval channel; =false executes immediately and
#      posts a confirmation there.
#
#  Query mode (anyone — read-only):
#    - Ask in the dedicated QUERY_CHANNEL_ID (no @-mention needed — every human
#      message is treated as a potential question), or @-mention the bot in a
#      monitored channel. If QUERY_CHANNEL_ID is unset, @-mention the bot in the
#      approval channel (fallback). A
#      lightweight LLM call (parse_query) decides is_query + a SOURCE scope
#      (discord | linear | both), then routes:
#        * source=discord -> the local Discord path: resolve the person and scan
#                            their recent activity in MONITORED channels (last
#                            QUERY_DISCORD_LOOKBACK_DAYS days); a model writes ONE
#                            concise summary. Linear is not touched.
#        * source=linear|both -> the TOOL-DRIVEN query engine (query_engine.py):
#                            the bot's own Claude call is handed READ-ONLY Linear
#                            tools and answers OPEN-ENDED questions with NO handler
#                            per phrasing — status, status-change history, due
#                            dates, priority/estimate/cycle, sub-issues, sorted
#                            lists, "what changed". Tools:
#                              - search_issues  (full-text: title+description+comments)
#                              - get_issue      (full fields incl. dueDate/estimate/cycle/parent/children)
#                              - get_issue_history (state/assignee/priority change timeline)
#                              - list_issues    (assignee/labels/state/dates/priority + sort)
#                              - resolve_member / list_team_members
#                              - source_message_for_issue / tracked_issue_for_message (linking)
#                            The loop is CAPPED at 5 tool iterations, then answers
#                            with what it has.
#    - DISCORD <-> LINEAR LINKING: the bot stores a message->issue mapping for
#      every issue it files/updates; the engine reads it both ways ("which discord
#      message is behind NFT2-591", or as a reply "is this already tracked?") and
#      replies with the issue AND the Discord jump link together. LIMITATION:
#      stored links exist ONLY for issues the bot itself created/updated; for a
#      manually-filed issue it says so plainly (no heuristic guessing).
#    - EDITED MESSAGES re-trigger QUERY MODE ONLY (never the report path):
#      editing an @-mention question re-answers on the new text with a fresh
#      reply; editing an ordinary message does nothing (no second ticket).
#    - DISCORD_LINEAR_MAP cross-links a person's Discord and Linear identities so
#      the discord-scoped path lines up both sides reliably (see below).
#    - Query mode (and edit-handling) runs BEFORE the report path and NEVER
#      creates, comments on, or modifies any Linear issue — read-only throughout.
#
# ============================================================================

# --- Discord ---------------------------------------------------------------
# Bot token from https://discord.com/developers/applications
DISCORD_TOKEN=

# Three distinct channel roles — keep them separate:
#   - MONITORED: report/triage. Allow-listed reporters' messages here get
#     classified into Linear tickets.
#   - APPROVAL: ✅/❌ only. Approval embeds are posted and reactions handled here.
#     Nothing else happens here.
#   - QUERY: ask/answer only. Questions are asked and answered here; no ticket
#     creation, no approvals.

# Comma-separated Discord channel IDs to monitor for incoming reports.
# The QUERY_CHANNEL_ID must NOT be one of these (it's query-only).
MONITORED_CHANNEL_IDS=

# Channel where the bot posts approval embeds and handles ✅/❌ (private
# staff-only channel). ONLY approvals happen here — it no longer answers queries.
APPROVAL_CHANNEL_ID=

# Dedicated channel for asking the bot questions (read-only query mode). In this
# channel the @-mention requirement is DROPPED — every non-bot human message is
# treated as a potential query and answered here. No ticket creation, no
# approvals. Leave empty to fall back to the previous behaviour: queries answered
# via @-mention in the APPROVAL_CHANNEL_ID. (Queries via @-mention in monitored
# channels keep working either way.)
QUERY_CHANNEL_ID=

# --- Reporter allowlist ----------------------------------------------------
# Only messages from these users get classified. IDs take priority over names.
# Leave both empty to disable triage (the bot will warn and act on nobody).

# Comma-separated Discord user IDs (numeric snowflakes).
ALLOWED_REPORTER_IDS=

# Comma-separated Discord display names (case-insensitive). Used as a fallback
# when a reporter's Discord ID is not yet known. Default: Sid,Harsh,Trishi.
ALLOWED_REPORTER_NAMES=Sid,Harsh,Trishi

# --- Discord ↔ Linear identity map -----------------------------------------
# JSON object mapping Discord user ID (string) → Linear user email or UUID.
# Used to assign / @-mention the reporter on the created Linear issue.
# Must be a valid JSON object; invalid JSON logs a warning and falls back to {}.
# Example: {"123456789":"sid@membrane.social","987654321":"harsh@nfthing.com"}
DISCORD_LINEAR_MAP={}

# --- Classifier (Anthropic) ------------------------------------------------
ANTHROPIC_API_KEY=
CLASSIFIER_MODEL=claude-sonnet-4-6

# --- Linear ----------------------------------------------------------------
LINEAR_API_KEY=
# UUID of the target Linear team (NFThing2.0).
LINEAR_TEAM_ID=

# --- Tuning ----------------------------------------------------------------
# Skip messages shorter than this (no attachments). Cuts classifier cost.
MIN_MESSAGE_LENGTH=20

# Drop classifier verdicts below this confidence (0.0–1.0).
MIN_CONFIDENCE=0.6

# Seconds to wait before classifying, so follow-ups (frontend vs backend,
# "nvm my mistake", etc.) can join the same thread context. 0 disables.
CLASSIFY_DELAY_SECONDS=0

# SQLite state file path.
DB_PATH=./bot_state.db

# --- Query mode (person-centric lookups) -----------------------------------
# For "what is <person> working on?" queries that combine Linear assignments
# with recent Discord activity. Both optional.

# How many days back to scan monitored channels for a person's Discord activity.
QUERY_DISCORD_LOOKBACK_DAYS=14

# Hard cap on messages scanned per monitored channel per query (bounds cost).
QUERY_MAX_MESSAGES_PER_CHANNEL=400

# --- Standup notes (read-only context for QUERY MODE only) -----------------
# A separate rclone process syncs Gemini standup notes from Google Drive into a
# LOCAL folder; the bot only READS local files and holds NO Google credential.
# Standup data is NEVER an input to ticket creation.
#
# Local folder rclone syncs the "Notes by Gemini" docs into. Empty/unset →
# standup features no-op gracefully (query mode just won't have standup context).
STANDUP_DIR=
# Optional shell command to force a sync before reading, used ONLY when a question
# is clearly about a recent/today standup (e.g. "what did we decide this morning").
# Empty → the bot reads whatever rclone has already synced to STANDUP_DIR.
# Example: STANDUP_SYNC_CMD=rclone copy gdrive:Standups ./standups --drive-export-formats docx
STANDUP_SYNC_CMD=

# --- Archive snapshot (read-only query-mode fallback) ----------------------
# A FROZEN local markdown file of past Done issues. When live Linear can't return
# an issue (archived / not found), query mode falls back to this snapshot — and
# labels every such answer "(from archive snapshot, through <date>)". NEVER an
# input to ticket creation. Empty → archive features no-op.
ARCHIVE_FILE=
# Example: ARCHIVE_FILE=./NFThing2_0_Consolidated_Done_Issues_Archive.md

# --- Holiday / leave channel (read-only context) ---------------------------
# Discord channel id where people post OOO / on-leave notes. Read-only context for
# query mode ("why was X delayed"). This channel is NOT monitored for triage and
# never produces a ticket. 0/unset → disabled.
HOLIDAY_CHANNEL_ID=

# --- Chief of Staff: identity & voice --------------------------------------
# The bot's name — the single identity it uses everywhere it speaks, asks a
# clarifying question, or nudges someone. Defaults to "Mira" when unset.
COS_NAME=Mira

# Speak as the named, first-person Chief of Staff on every REPLY path (queries,
# clarifying questions, follow-up nudges). This changes the VOICE only — never
# what the bot does, what it's allowed to do, or any team convention.
# false → prompts fall back to their original neutral voice.
COS_PERSONA_ENABLED=true

# --- Chief of Staff: query-mode memory -------------------------------------
# Short-term, IN-MEMORY (lost on restart) context per channel, so a follow-up
# question ("what about Ravi?") carries the previous question's intent forward.
# Never persisted, and never an input to ticket creation.
#
# How many recent (question, answer) turns to keep per channel.

# --- Chief of Staff: clarifying questions (before a ticket is filed) --------
# When a report is missing something a good ticket can't do without, she asks the
# ORIGINAL REPORTER (by @mention, in the report's channel) instead of filing a
# half-complete ticket. The plan is PARKED and reaches Linear only via the normal
# approval gate — after the answer lands, or (on timeout) filed flagged needs-triage.
# READ/PROPOSE ONLY — never writes to Linear.

# Ask ONE focused question, or bundle a few genuinely-needed ones into ONE message.
COS_CLARIFY_ENABLED=true
# Max questions bundled into that one message (a short numbered list). 1 = strict
# single-question behaviour; never fires multiple messages regardless.
COS_CLARIFY_MAX_QUESTIONS=3
# Reporters often post the screenshot/recording a moment AFTER the text. Before asking,
# she waits this many seconds and re-reads the thread; if a follow-up attachment/message
# arrived she re-assesses (and may no longer need to ask). 0 disables the wait.
COS_CLARIFY_ATTACHMENT_WAIT_SECONDS=45
# How long she waits for an answer before filing the parked plan as-is, flagged
# needs-triage (never dropped, never re-pinged endlessly).
COS_CLARIFY_TIMEOUT_MINUTES=120
# How long an unanswered question sits before the sweeper nudges the reporter ONCE
# (subject to the shared nudge window/attempts below). Kept under the timeout above.
COS_CLARIFY_NUDGE_AFTER_MINUTES=60
# Below this model confidence the gap is ignored and the plan goes straight to approval.
COS_CLARIFY_MIN_CONFIDENCE=0.6

# --- Chief of Staff: proactive follow-up / nudging -------------------------
# She notices commitments people make in channel — "I'll confirm the DM dates in an
# hour", "DMs going live tomorrow", "I'll release/deploy/push it", "will be done by
# Friday" — tracks them as open items (who, what, when promised, source message), and
# nudges once they age past due. NEVER creates or modifies a ticket — nudge only.
COS_FOLLOWUP_ENABLED=true
# How often the background sweeper runs (open threads, clarification nudges, timeouts).
COS_FOLLOWUP_CHECK_INTERVAL_MINUTES=30
# Due time for a promise with NO stated deadline ("will update after testing"). A stated
# time always wins.
COS_FOLLOWUP_DEFAULT_DUE_MINUTES=180
# Only track a commitment the model is at least this sure about — a false nudge is worse
# than a missed one.
COS_FOLLOWUP_MIN_CONFIDENCE=0.6
# Legacy per-open-thread guardrails. Still honoured, but the UNIFIED policy below governs
# both nudge sources (the effective cooldown is the LONGER, the cap the SMALLER — so
# turning the unified policy on never makes her more chatty).
COS_FOLLOWUP_MAX_REMINDERS=2
COS_FOLLOWUP_REMINDER_COOLDOWN_MINUTES=180

# UNIFIED NUDGE POLICY — governs BOTH sources (tracked commitments AND unanswered
# clarifications). Nudge the same person about the same item at most ONCE per window,
# at most N attempts, then stop, record the non-response in the DB, and tell the PMs
# (ESCALATION_USER_IDS) ONCE — "I've asked @Ravi twice about the DM date with no reply."
COS_NUDGE_WINDOW_HOURS=24
COS_NUDGE_MAX_ATTEMPTS=2
# (The PM give-up notice uses ESCALATION_USER_IDS and the escalation channel — both set
#  in the escalation section below.)

# --- Chief of Staff: tagging & escalation ladder ---------------------------
# The bot audits the team's ACTIVE Linear issues each sweep and asks the right human
# about gaps a Chief of Staff would chase. Two levels:
#   Level 1 → the ASSIGNEE, for a data gap on their own issue (missing due date, no
#             repro, vague scope).
#   Level 2 → the PMs (ESCALATION_USER_IDS), for a call above an IC (stuck-and-silent,
#             a comment-evidenced blocker needing a decision, an unowned launch issue).
# Every nudge is READ-ONLY (a Discord @-mention) EXCEPT the opt-in deadline write below.

# Master on/off for each level (true/false). Each rung is independent.
COS_TAG_ASSIGNEE_ENABLED=true
COS_ESCALATE_ENABLED=true

# Discord user IDs of the PMs (Trishi, Kushal) — the ONLY people a level-2 escalation
# may @-mention. Comma-separated. Empty → level-2 escalations are skipped.
ESCALATION_USER_IDS=

# SCOPE: only issues whose workflow-state NAME is one of these are ever considered for
# tagging — In Progress / Implemented (a.k.a. "awaiting QA", dev-done not yet tested) /
# In Review. Backlog, Todo, Done, and Canceled are ignored. Matched by NAME
# (case-insensitive) because these states share the "started"/"completed" TYPE.
# Comma-separated; override to your workspace's exact state names. The default lists both
# "Implemented" and "awaiting QA" since the dev-done state is named differently across
# workspaces.
COS_ACTIVE_STATE_NAMES=In Progress,Implemented,awaiting QA,In Review

# CHECK COMMENTS BEFORE TAGGING (read-only). Before pinging an assignee, read the issue's
# comments: if a recent comment already answers what she'd ask (e.g. a committed deadline)
# she doesn't tag; if the comments show a blocker / needs-a-decision / knowledge-transfer
# gap, she escalates the REASON to the PMs instead of nagging the IC. Default true.
COS_CHECK_COMMENTS_ENABLED=true

# CLOSE THE LOOP — the ONLY Linear WRITE this bot performs, and it is OPT-IN (default
# false). After she tags an assignee about a missing due date, she watches for the answer
# (their Discord REPLY to the tag, a new issue COMMENT, or the STANDUP notes), parses a
# date, and updates the ticket. She may ONLY add a comment and set/update the DUE DATE —
# never a status change or reassignment.
COS_UPDATE_DEADLINE_ENABLED=false

# DRY-RUN guard for the write above. true (default, even when the feature is enabled) →
# she LOGS "would set NFT2-591 due 2026-07-18 (source: Discord reply)" and writes NOTHING,
# so you can test the resolution safely. Set false only to arm real writes.
COS_UPDATE_DEADLINE_DRY_RUN=true

# How many days a deadline watch waits for an answer before it's given up on (expired),
# so an unanswered ask doesn't get re-checked forever.
COS_DEADLINE_WATCH_EXPIRE_DAYS=7

# An issue In Progress this many days with NO state change AND no comment explaining the
# holdup is "stuck" — a level-2 PM call, not an IC nudge.
STALE_IN_PROGRESS_DAYS=3

# A project target date this close (or already past) makes its issues launch-critical —
# the highest-value nudges (missing due date, unowned) fire hardest inside this window.
COS_LAUNCH_WINDOW_DAYS=7

# RATE LIMIT: never tag the same person about the same issue for the same reason twice
# inside this many hours. Fails closed (a broken check → stay quiet).
COS_TAG_COOLDOWN_HOURS=24

# Hard cap on nudges posted per sweep, so a messy backlog can't carpet-bomb the channel.
COS_MAX_NUDGES_PER_SWEEP=3

# Where nudges/escalations are posted. Empty → the first MONITORED_CHANNEL_IDS channel.
COS_NUDGE_CHANNEL_ID=

# Hard cap on issues pulled per audit pass (bounds Linear + LLM cost).
COS_AUDIT_MAX_ISSUES=50

# --- Approval flow ---------------------------------------------------------
# Require human ✅ before creating the Linear issue. Accepts true/false/1/0/yes/no.
# Set to false for auto-create (no approval embed, no reaction step).
REQUIRE_APPROVAL=true

# --- Logging ---------------------------------------------------------------
# DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
